def variabel(n):
  variable = x + 1
  return variable

def pangkat(x):
  Pangkat = 10 ** x

  return Pangkat

n = int(input("Masukkan Nilai yang anda inginkan : "))

for x in range (n):
  if pangkat(x) < n:
      variabel(n)

  elif pangkat(x) > n:
    print(pangkat(x))
    break
